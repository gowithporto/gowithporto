# API — superseded

> **This document is no longer maintained.** It is superseded by
> [`06-API.md`](06-API.md), which covers the endpoint reference, contracts and idempotency strategy and is
> verified against the codebase at commit `3eb178a`.
>
> Superseded content is preserved in git history.

See [`00-INDEX.md`](00-INDEX.md) for the full documentation map.

**This file should be removed** (`git rm docs/API.md`). It is left as a stub only
so that existing links do not lead a reader to stale content.
